image: openjdk:8

# The docker image needs to contain scala-cli.

compile:
  stage: build
  script:
    - echo "AUTOMATED COMPILATION NOT IMPLEMENTED"

test:
  stage: test
  needs: [compile]
  script:
    - echo "AUTOMATED TESTING NOT IMPLEMENTED"